node_modules/
.env
uploads/**
!uploads/**/.gitkeep
storage/

# Raw customer PII dumped by scripts/exportData.js during the MySQL -> Neon
# move (names, phones, addresses, order history) — never belongs in git.
prisma/data-export.json
